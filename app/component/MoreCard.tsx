import React from "react";
interface MoreProps {
  title?: string;
  subTitle?: string;
  logo?: any;
}
const MoreCard = ({ title, subTitle, logo }: MoreProps) => {
  return <div>MoreCard</div>;
};

export default MoreCard;
